apiVersion: v1
kind: Service
metadata:
  name: register-svc
spec:
  ports:
    - port: 8090
      targetPort: 80
      nodePort: 30001
      protocol: TCP
  selector:
    app: register
  type: NodePort
